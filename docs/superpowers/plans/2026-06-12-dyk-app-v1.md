# DYK App v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bygg om palma_app till Did You Know! (DYK) enligt designspec — 5 tabs, onboarding, kategorier, Supabase-data, sparade platser och exploring-toggle.

**Architecture:** Flutter-appen får ny struktur: `theme/` (DYK-designsystem), `models/` (kategori-medvetna), `services/` (Supabase-repository ersätter lokal JSON; geofencing styrs av ExploringState), `screens/onboarding/` + `screens/tabs/`. Supabase är källa för allt innehåll; admin-panelen matar in det.

**Tech Stack:** Flutter, supabase_flutter, Mapbox, geofencing_api, just_audio, flutter_local_notifications, shared_preferences

**Spec:** `docs/superpowers/specs/2026-06-12-dyk-app-design.md`

---

### Task 1: SQL-migration — kategorier, koder, user-tabeller

**Files:**
- Create: `supabase/migrations/005_dyk_v1.sql`

- [ ] **Steg 1: Skriv migrationen**

```sql
-- Kategorier på hotspots
alter table public.hotspots add column category text not null default 'history'
  check (category in ('history', 'funfact', 'headline'));

-- Inlösenkod på deals
alter table public.hot_deals add column redeem_code text;

-- Användarens valda intressen
create table public.user_interests (
  user_id uuid not null references auth.users(id) on delete cascade,
  interest text not null check (interest in ('history', 'funfact', 'headline', 'hotdeal')),
  primary key (user_id, interest)
);
alter table public.user_interests enable row level security;
create policy "own_interests" on public.user_interests
  for all using (user_id = auth.uid()) with check (user_id = auth.uid());

-- Sparade objekt (hotspot eller deal)
create table public.saved_items (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  item_type text not null check (item_type in ('hotspot', 'deal', 'citypack')),
  item_id uuid not null,
  created_at timestamptz not null default now(),
  unique(user_id, item_type, item_id)
);
alter table public.saved_items enable row level security;
create policy "own_saved" on public.saved_items
  for all using (user_id = auth.uid()) with check (user_id = auth.uid());

-- Besökslogg (för stats + achievements)
create table public.user_visits (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  hotspot_id uuid not null references public.hotspots(id) on delete cascade,
  visited_at timestamptz not null default now()
);
alter table public.user_visits enable row level security;
create policy "own_visits" on public.user_visits
  for all using (user_id = auth.uid()) with check (user_id = auth.uid());

-- Uppdatera seed: kategorier på Palmas hotspots
update public.hotspots set category = 'history' where slug in ('catedral', 'placa_cort');
update public.hotspots set category = 'funfact' where slug = 'placa_major';
update public.hot_deals set redeem_code = 'PALMA25' where business_name = 'Restaurang La Bóveda';
```

- [ ] **Steg 2: Kör i Supabase SQL Editor** (https://supabase.com/dashboard/project/jqykkyhoxpykhixwgwyw/sql/new) — Förväntat: "Success. No rows returned"

- [ ] **Steg 3: Commit**

```bash
git add supabase/migrations/005_dyk_v1.sql
git commit -m "feat(db): categories, redeem codes, user interests/saved/visits"
```

---

### Task 2: Admin-panelen — kategori + kod-fält

**Files:**
- Modify: `C:\Users\tilly\dyk-admin\src\supabase.ts` (lägg `category` på Hotspot, `redeem_code` på HotDeal)
- Modify: `C:\Users\tilly\dyk-admin\src\pages\HotspotsPage.tsx`
- Modify: `C:\Users\tilly\dyk-admin\src\pages\DealsPage.tsx`

- [ ] **Steg 1: Typer** — i `supabase.ts` lägg till `category: 'history' | 'funfact' | 'headline'` i Hotspot-interfacet och `redeem_code: string | null` i HotDeal.

- [ ] **Steg 2: HotspotsPage** — lägg till select i formuläret:

```tsx
<div>
  <label>Category</label>
  <select value={editing.category ?? 'history'} onChange={e => setEditing({ ...editing, category: e.target.value as Hotspot['category'] })}>
    <option value="history">🏛️ History</option>
    <option value="funfact">💡 Fun Fact</option>
    <option value="headline">📰 Headline</option>
  </select>
</div>
```

Inkludera `category` i save-raden och visa badge i tabellen.

- [ ] **Steg 3: DealsPage** — lägg till textfält "Redeem code" (`redeem_code`), inkludera i save, visa i tabellen.

- [ ] **Steg 4: Verifiera** — öppna http://localhost:5173, redigera en hotspot, sätt kategori, spara. Kontrollera i Supabase Table Editor.

- [ ] **Steg 5: Commit** (i dyk-admin-repot)

---

### Task 3: Flutter — paket & DYK-tema

**Files:**
- Modify: `pubspec.yaml` (lägg `supabase_flutter: ^2.8.0`, `shared_preferences: ^2.2.0`)
- Create: `lib/theme/dyk_theme.dart`

- [ ] **Steg 1: pubspec** — lägg till paketen, kör `flutter pub get`.

- [ ] **Steg 2: Tema-fil**

```dart
import 'package:flutter/material.dart';

class DykColors {
  static const yellow = Color(0xFFFFC107);
  static const black = Color(0xFF1A1A1A);
  static const cream = Color(0xFFFAF6EE);
  static const green = Color(0xFF22C55E);
}

ThemeData dykLightTheme() => _base(Brightness.light);
ThemeData dykDarkTheme() => _base(Brightness.dark);

ThemeData _base(Brightness b) {
  final dark = b == Brightness.dark;
  return ThemeData(
    useMaterial3: true,
    brightness: b,
    scaffoldBackgroundColor: dark ? DykColors.black : DykColors.cream,
    colorScheme: ColorScheme.fromSeed(
      seedColor: DykColors.yellow,
      brightness: b,
      primary: DykColors.yellow,
    ),
    elevatedButtonTheme: ElevatedButtonThemeData(
      style: ElevatedButton.styleFrom(
        backgroundColor: DykColors.yellow,
        foregroundColor: DykColors.black,
        textStyle: const TextStyle(fontWeight: FontWeight.w800, letterSpacing: 0.5),
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
        padding: const EdgeInsets.symmetric(vertical: 16, horizontal: 24),
      ),
    ),
  );
}
```

- [ ] **Steg 3: Commit**

---

### Task 4: Modeller — kategori & deals

**Files:**
- Modify: `lib/models/hotspot.dart` (lägg `category`, `isFree`, `citypackId`; id blir uuid-sträng från Supabase)
- Create: `lib/models/hot_deal.dart`
- Create: `lib/models/city_pack.dart`
- Test: `test/models_test.dart`

- [ ] **Steg 1: Hotspot** — lägg `final String category;` + `final bool isFree;`, parse från json med default `'history'`/`false`.

- [ ] **Steg 2: HotDeal**

```dart
class HotDeal {
  final String id, businessName, offerText;
  final String? redeemCode;
  final double lat, lng;
  final int radiusMeters;

  HotDeal({required this.id, required this.businessName, required this.offerText,
    this.redeemCode, required this.lat, required this.lng, required this.radiusMeters});

  factory HotDeal.fromJson(Map<String, dynamic> json) => HotDeal(
    id: json['id'] as String,
    businessName: json['business_name'] as String,
    offerText: json['offer_text'] as String,
    redeemCode: json['redeem_code'] as String?,
    lat: (json['lat'] as num).toDouble(),
    lng: (json['lng'] as num).toDouble(),
    radiusMeters: json['radius_meters'] as int,
  );
}
```

- [ ] **Steg 3: CityPack** — id, name, city, country, description, priceSek, fromJson på samma mönster.

- [ ] **Steg 4: Tester** — fromJson-test per modell. Kör `flutter test`. Commit.

---

### Task 5: SupabaseRepository — ersätter lokal JSON

**Files:**
- Create: `lib/services/dyk_repository.dart`
- Modify: `lib/main.dart` (init Supabase)

- [ ] **Steg 1: Init i main**

```dart
await Supabase.initialize(
  url: 'https://jqykkyhoxpykhixwgwyw.supabase.co',
  anonKey: 'sb_publishable_HCRDnKdePmnT0ukTyVlFCg_q0MRPyhK',
);
```

- [ ] **Steg 2: Repository**

```dart
class DykRepository {
  final SupabaseClient _client;
  DykRepository({SupabaseClient? client}) : _client = client ?? Supabase.instance.client;

  Future<List<CityPack>> loadCityPacks() async {
    final rows = await _client.from('citypacks').select().eq('is_published', true);
    return rows.map<CityPack>((r) => CityPack.fromJson(r)).toList();
  }

  Future<List<Hotspot>> loadHotspots(String citypackId) async {
    final rows = await _client.from('hotspots').select()
        .eq('citypack_id', citypackId).order('sort_order');
    return rows.map<Hotspot>((r) => Hotspot.fromJson(r)).toList();
  }

  Future<List<HotDeal>> loadDeals(String citypackId) async {
    final rows = await _client.from('hot_deals').select()
        .eq('citypack_id', citypackId).eq('is_active', true);
    return rows.map<HotDeal>((r) => HotDeal.fromJson(r)).toList();
  }
}
```

- [ ] **Steg 3: Fallback** — om Supabase-anropet kastar (offline), logga och returnera tom lista; appen får inte krascha utan nät.

- [ ] **Steg 4: Verifiera med `flutter run -d chrome`** att hotspots laddas från Supabase. Commit.

---

### Task 6: ExploringState + intressen (lokal persistens)

**Files:**
- Create: `lib/services/app_state.dart`
- Test: `test/app_state_test.dart`

- [ ] **Steg 1: State-klass**

```dart
class AppState extends ChangeNotifier {
  final SharedPreferences _prefs;
  AppState(this._prefs);

  bool get isExploring => _prefs.getBool('exploring') ?? false;
  Set<String> get interests =>
      (_prefs.getStringList('interests') ?? ['history', 'funfact', 'headline', 'hotdeal']).toSet();
  bool get onboardingDone => _prefs.getBool('onboarding_done') ?? false;

  Future<void> setExploring(bool v) async { await _prefs.setBool('exploring', v); notifyListeners(); }
  Future<void> toggleInterest(String i) async {
    final s = interests; s.contains(i) ? s.remove(i) : s.add(i);
    await _prefs.setStringList('interests', s.toList()); notifyListeners();
  }
  Future<void> completeOnboarding() async { await _prefs.setBool('onboarding_done', true); notifyListeners(); }
}
```

- [ ] **Steg 2: Tester** med `SharedPreferences.setMockInitialValues({})`. Kör `flutter test`. Commit.

---

### Task 7: Geofencing-styrning — exploring + kategorifilter + deals

**Files:**
- Modify: `lib/services/geo_fencing_service.dart`

- [ ] **Steg 1:** `startMonitoring` tar nu både hotspots och deals (deals blir regioner med id-prefix `deal_`). `onDealEnter`-stream läggs till bredvid `onHotspotEnter`.

- [ ] **Steg 2:** Notislogiken (i main/AppShell): trigga endast om `appState.isExploring` och hotspot-kategorin (eller `hotdeal`) finns i `appState.interests`. STOP EXPLORING → `geoService.stop()`, START → `startMonitoring` igen.

- [ ] **Steg 3:** Deal-notis: titel `🔥 ${deal.businessName} — ${deal.offerText}`, body visar koden om den finns.

- [ ] **Steg 4:** `flutter analyze` + commit.

---

### Task 8: Onboarding-flödet (5 skärmar)

**Files:**
- Create: `lib/screens/onboarding/welcome_screen.dart`
- Create: `lib/screens/onboarding/interests_screen.dart`
- Create: `lib/screens/onboarding/location_screen.dart`
- Create: `lib/screens/onboarding/notifications_screen.dart`
- Create: `lib/screens/onboarding/ready_screen.dart`

- [ ] **Steg 1: Welcome** — DYK-logo (assets/images/dyk_logo.png), tagline "Explore cities. Discover stories. Unlock hidden gems.", gul START EXPLORING-knapp → interests.
- [ ] **Steg 2: Interests** — 4 valbara kort (History/Fun Facts/Headlines/Hot Deals) med badge-bild, titel, beskrivning, ✓-overlay vid val. Continue sparar via `appState`.
- [ ] **Steg 3: Location** — text + Enable Location → `Permission.locationAlways.request()` → nästa oavsett svar (visa varning vid nekande).
- [ ] **Steg 4: Notifications** — samma mönster med `Permission.notification`.
- [ ] **Steg 5: Ready** — "You're all set." + Let's Go → `appState.completeOnboarding()` + `setExploring(true)` → AppShell.
- [ ] **Steg 6:** Bilder: lägg badge-PNG:er i `assets/images/badges/` (Alexander levererar; tills dess emoji-fallback). Commit.

---

### Task 9: AppShell — 5 tabs + global header

**Files:**
- Create: `lib/screens/app_shell.dart`
- Modify: `lib/main.dart`

- [ ] **Steg 1: Shell**

```dart
class AppShell extends StatefulWidget { /* services + appState in */ }

// _AppShellState:
// - IndexedStack med ExploreTab, NearbyTab, CityPacksTab, SavedTab, ProfileTab
// - BottomNavigationBar type: fixed, 5 items med ikoner enligt design
//   (map / location pin / buildings / heart / person), gul selected-färg
// - AppBar-ersättare: egen header-rad med profilcirkel, logo-bild, klocka
```

- [ ] **Steg 2: main.dart** — `home:` blir `appState.onboardingDone ? AppShell(...) : WelcomeScreen(...)`. Geofencing startas endast om exploring är på och tillstånd finns.

- [ ] **Steg 3:** `flutter analyze`, kör i Chrome för layoutkoll. Commit.

---

### Task 10: Explore-tab

**Files:**
- Create: `lib/screens/tabs/explore_tab.dart`
- Create: `lib/widgets/exploring_button.dart`
- Create: `lib/widgets/interest_badge_row.dart`
- Create: `lib/widgets/notification_card.dart`

- [ ] **Steg 1: ExploringButton** — stor gul knapp; exploring på = "EXPLORING — TAP TO PAUSE" (gul, puls-animation), av = "START EXPLORING" (grå-svart). Tap → `appState.setExploring(!...)` + starta/stoppa geofencing.
- [ ] **Steg 2: InterestBadgeRow** — horisontell rad av badges med ✓-bock på aktiva, tap togglar via appState (uppdaterar notisfilter direkt).
- [ ] **Steg 3: Mini-karta** — 200px Mapbox-vy, ej interaktiv, pins per kategori, tap → byt till Nearby-tabben.
- [ ] **Steg 4: Latest notifications** — lista från lokal notislogg (spara senaste 20 triggade i SharedPreferences som json-lista). Hotspot-kort: kategori-label + avstånd + namn + beskrivning → detaljsida. Deal-kort: CODE-chip.
- [ ] **Steg 5:** Commit.

---

### Task 11: Nearby-tab — fullkarta med kategori-pins

**Files:**
- Create: `lib/screens/tabs/nearby_tab.dart` (ersätter map_screen.dart)

- [ ] **Steg 1:** Flytta Mapbox-logiken från `map_screen.dart`, lägg till: sökfält (filtrerar hotspots på namn), kategorifilter-rad (samma badges, döljer/visar pins), lokaliserings-FAB.
- [ ] **Steg 2: Custom markers** — ladda badge-PNG per kategori via `annotationManager` `iconImage`. Fallback: färgade cirklar per kategori tills bilderna finns.
- [ ] **Steg 3:** Pin-tap → HotspotDetailScreen. Ta bort gamla `map_screen.dart` + `list_screen.dart` (listan ersätts av Saved/sök). Commit.

---

### Task 12: Hotspot-detaljsida — DYK-stil + save

**Files:**
- Modify: `lib/screens/hotspot_detail_screen.dart`

- [ ] **Steg 1:** Uppdatera till DYK-tema: kategori-badge överst, gul accent, save-bokmärke i appbaren (sparar lokalt i v1: `saved_hotspots`-lista i SharedPreferences; synk till Supabase när auth kommer).
- [ ] **Steg 2:** Audio-player oförändrad men ljud-URL kan nu vara Supabase Storage public URL (`hotspot_media`). Bilder via `Image.network` med fallback-gradient.
- [ ] **Steg 3:** Commit.

---

### Task 13: City Packs-tab

**Files:**
- Create: `lib/screens/tabs/city_packs_tab.dart`

- [ ] **Steg 1:** Tabs All/Downloaded/Available (Downloaded = packs vars hotspots redan hämtats och cachats lokalt — v1: markera aktiv stad som "downloaded" när hotspots laddats).
- [ ] **Steg 2:** Pack-kort enligt design: bild (Supabase Storage eller nät-URL), namn versalt, land, antal hotspots, beskrivning, gul DOWNLOAD-knapp → laddar hotspots+deals för packen och sätter den som aktiv stad (geofencing byter regioner).
- [ ] **Steg 3:** Commit.

---

### Task 14: Saved-tab + Profile-tab (v1-nivå)

**Files:**
- Create: `lib/screens/tabs/saved_tab.dart`
- Create: `lib/screens/tabs/profile_tab.dart`

- [ ] **Steg 1: Saved** — filterchips All/Places/Deals, kort-grid från lokalt sparade hotspots/deals. Tom-state: "Nothing saved yet — tap the bookmark on any place."
- [ ] **Steg 2: Profile** — utan auth i v1: lokal statistik (visited count från lokal besökslogg, saved count, downloaded packs), achievements-rad statiskt (lås alla utom de som uppfylls lokalt), menyrad med Settings (tema-val), About. Go Premium-banner visas men View Plans → "Coming soon"-dialog.
- [ ] **Steg 3:** Commit.

---

### Task 15: Rebrand + bygge

**Files:**
- Modify: `android/app/src/main/AndroidManifest.xml` (`android:label="Did You Know?"`)
- Modify: `pubspec.yaml` (name behålls tekniskt, description uppdateras)
- Modify: `lib/main.dart` (title: 'Did You Know?')

- [ ] **Steg 1:** Byt app-label, titel och ta bort kvarvarande "Palma Explorer"-strängar (`grep -ri "palma explorer" lib/`).
- [ ] **Steg 2:** App-ikon: lägg `assets/icon/dyk_icon.png` (Alexander levererar) + `flutter_launcher_icons` — hoppa över om bilden saknas.
- [ ] **Steg 3:** `flutter analyze` (0 errors), `flutter test` (alla gröna).
- [ ] **Steg 4:** `flutter build apk --debug` → Förväntat: `√ Built build\app\outputs\flutter-apk\app-debug.apk`
- [ ] **Steg 5:** Commit + tagga `v1.0.0-dyk-alpha`.

---

## Självgranskning

- Spec-täckning: onboarding (T8), 5 tabs (T9–T14), kategorier (T1/T4/T7), exploring-toggle (T6/T7/T10), deals med koder (T1/T2/T7/T10), City Packs download (T13), Saved (T14), tema (T3), rebrand (T15). Premium/auth/Articles/offline-zip explicit utanför v1 enligt spec.
- Kända luckor (medvetna): badge-bilder och app-ikon kräver Alexanders assets; Saved synkas lokalt tills auth byggs.
